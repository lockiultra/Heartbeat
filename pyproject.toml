[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "heartbeat"
version = "0.1.0"
description = "Initial bootstrap for the AI Copilot for doctors project."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.setuptools.packages.find]
where = ["backend/src"]
include = ["app*"]
